#include <iostream>
#include <queue>

using namespace std;

int main(){
    const int N = 6;
    queue<pair<double, double>> q;
    int n4 = 1;
    for(int i = 0; i < N; i++)n4 *= 4;
    q.push({0, 0});
    q.push({800, 0});
    for(int i = 0; i < (n4 - 1) / 3; i++){
        
    }
    return 0;
}